project_id = "lqhwbwmlvtrpovexiyye"

[functions.process-recurring]
# Require a valid JWT — the app invokes this on load (with the user's session),
# so it no longer needs to be a public endpoint.
verify_jwt = true

[functions.generate-meal-plan]
verify_jwt = true

[functions.financial-insights]
# Gated to authenticated household sessions; runs with service role internally.
verify_jwt = true